Perfect 🔥 Here’s your **💻 2-Day OOP in JavaScript Master Plan** — short, focused, and beginner-friendly, using *normal JavaScript (ES6)* only.

You’ll go from **basic objects → full class system → real mini project** in just 2 days.

---

## 🗓️ **Day 1 — Core OOP Concepts**

🎯 **Goal:** Understand how OOP works and practice classes, objects, and methods.

---

### 🧩 1. Learn the OOP Basics

* What OOP means (object-oriented programming)
* 4 main pillars:
  **Encapsulation**, **Abstraction**, **Inheritance**, **Polymorphism**

🧠 Think of:

* Class = blueprint
* Object = house built from the blueprint

---

### 🧩 2. Practice Basic Object and Class

**Object literal**

```js
const person = {
  name: "Ali",
  speak() {
    console.log(`Hi, I'm ${this.name}`);
  }
};
person.speak();
```

**Class and constructor**

```js
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  introduce() {
    console.log(`I'm ${this.name}, ${this.age} years old.`);
  }
}

const p1 = new Person("Ali", 20);
p1.introduce();
```

✅ Learn:

* `constructor()`
* `this`
* `new` keyword

---

### 🧩 3. Add Methods and Properties

Add new functions to a class:

```js
class Car {
  constructor(brand, color) {
    this.brand = brand;
    this.color = color;
  }

  start() {
    console.log(`${this.brand} is starting...`);
  }
}

const car1 = new Car("Toyota", "Red");
car1.start();
```

---

### 🧩 4. 🧠 Practice Exercise

Create a **Phone** class with:

* Properties: brand, model, price
* Method: `call()` → prints “Calling from [brand] [model]”

✅ **Goal:** build 2 phones and call each one.

---

### 🧠 End of Day 1 — You should know:

✔ Classes
✔ Objects
✔ `constructor`
✔ Methods & `this`

---

## 🗓️ **Day 2 — Advanced OOP: Inheritance, Private, and Project**

🎯 **Goal:** Build relationships between classes and make one mini project.

---

### 🧩 1. Inheritance (Extend)

```js
class Animal {
  speak() {
    console.log("Animal makes a sound");
  }
}

class Dog extends Animal {
  speak() {
    console.log("Dog barks");
  }
}

const dog = new Dog();
dog.speak(); // Dog barks
```

✅ Learn:

* `extends` → inherits parent class
* `super()` → call parent constructor

---

### 🧩 2. Encapsulation (Private Fields)

Hide internal data:

```js
class BankAccount {
  #balance = 0;

  deposit(amount) {
    this.#balance += amount;
  }

  getBalance() {
    return this.#balance;
  }
}

const acc = new BankAccount();
acc.deposit(100);
console.log(acc.getBalance()); // 100
```

---

### 🧩 3. Polymorphism (Override)

```js
class Shape {
  draw() {
    console.log("Drawing a shape");
  }
}

class Circle extends Shape {
  draw() {
    console.log("Drawing a circle");
  }
}

new Circle().draw(); // Drawing a circle
```

---

### 🧩 4. Mini Project: School Management (Combine All)

**Goal:** Use everything you learned in one example.

```js
class Person {
  constructor(name, age) {
    this.name = name;
    this.age = age;
  }

  introduce() {
    console.log(`Hi, I'm ${this.name}, ${this.age} years old.`);
  }
}

class Student extends Person {
  constructor(name, age, grade) {
    super(name, age);
    this.grade = grade;
  }

  study() {
    console.log(`${this.name} is studying in grade ${this.grade}.`);
  }
}

class Teacher extends Person {
  constructor(name, age, subject) {
    super(name, age);
    this.subject = subject;
  }

  teach() {
    console.log(`${this.name} is teaching ${this.subject}.`);
  }
}

const s1 = new Student("Aisha", 19, "12th");
const t1 = new Teacher("Mr. Omar", 40, "Math");

s1.introduce();
s1.study();
t1.introduce();
t1.teach();
```

✅ **Concepts used:**

* Class & object
* Constructor
* Inheritance
* Method overriding
* Reuse with `super()`

---

### 🧠 End of Day 2 — You can now:

✔ Build and use classes
✔ Create reusable blueprints
✔ Use inheritance
✔ Protect data with private fields
✔ Build a small OOP project

---

Would you like me to make a **small quiz + coding challenges** (10 questions) to test your understanding after this 2-day plan?
